
events {}
http {
    upstream my_app {
        server server:8000;   # use "server", not "app"
    }

    server {
        listen 80;
        location / {
            proxy_pass http://my_app;
        }
    }
}
